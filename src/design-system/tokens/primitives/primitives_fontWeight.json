{
  "$schema": "https://tandem.design/schemas/tokens/primitives/fontWeight.json",
  "$description": "Font weight primitives - Raw numeric values with named aliases. Mobile and desktop use same weights.",
  "$version": "2.0.0",
  "$hierarchy": "THIS FILE (raw numbers) → semantic/typography.json → components/*.json",
  "$rule": "ONLY numeric values - NO usage guidance",

  "100": 100,
  "200": 200,
  "300": 300,
  "400": 400,
  "500": 500,
  "600": 600,
  "700": 700,
  "800": 800,
  "900": 900,

  "$aliases": {
    "thin": 100,
    "extralight": 200,
    "light": 300,
    "normal": 400,
    "regular": 400,
    "medium": 500,
    "semibold": 600,
    "bold": 700,
    "extrabold": 800,
    "black": 900
  },

  "$tailwindMapping": {
    "100": "font-thin",
    "200": "font-extralight",
    "300": "font-light",
    "400": "font-normal",
    "500": "font-medium",
    "600": "font-semibold",
    "700": "font-bold",
    "800": "font-extrabold",
    "900": "font-black"
  },

  "$mobileConsiderations": {
    "note": "Font weights render consistently across devices. Same values used for mobile and desktop.",
    "recommendation": "Avoid weights below 400 on mobile for body text (poor rendering on low-DPI screens)",
    "optimalRange": "400-700 for best cross-platform rendering"
  }
}
